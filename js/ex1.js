class Circle {
    constructor(perimeter, area, diagonal) {
      this.perimeter = perimeter;
      this.diagonal = diagonal;
      this.side = 2;
    }

    findPerimeter(one) {
        this.perimeter = side * 4;
    }
    findArea(two) {
        this.area = side ^ 2;
    }
    findDiagnonal(three) {
        this.diagonal = Math.sqrt(2 * side);
    }
    Describe() {
        return `${this.perimeter}, ${this.area}, ${this.diagonal};
    }

console.log(side.describe());